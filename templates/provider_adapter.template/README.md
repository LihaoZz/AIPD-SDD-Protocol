# {{provider_id}} Adapter

This adapter exposes a minimal Codex-app-server-compatible stdio interface for
provider `{{provider_id}}`.

Entry point:

```bash
python3 runtime/{{provider_id}}_adapter/bin/{{provider_id}}_app_server.py app-server
```
