# Quality Memory

## Purpose

Store only reusable quality lessons across function blocks and mission blocks.

## Stable Rules

- `rule`:
- `why`:
- `source_refs`:

## Repeated Failure Patterns

- `pattern`:
- `seen_in`:
- `avoid_next_time`:

## Validated Good Patterns

- `pattern`:
- `use_when`:
- `evidence_refs`:

## High-Risk Areas

- `area`:
- `common_failure`:
- `required_checks`:

## Recent Lessons

- `date`:
- `lesson`:
- `source_mb`:

## Usage Rules

- Do not use this file as a running log of every MB.
- Treat this file as the human-readable bridge from runtime memory, not as the machine memory source of truth.
- Record only reusable lessons, repeated failures, stable good patterns, and high-risk reminders.
- Update this file when:
  a repeated failure appears,
  a new high-value lesson is confirmed,
  or a rule should become stable.
- Do not record routine success with no reusable insight.
- Every lesson should point back to a concrete `fb` or `mb` reference when possible.

---
